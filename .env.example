# Anon-sleutel volstaat: de database bewaakt zichzelf (RLS + is_admin()).
# De sleutel is niet geheim — hij staat straks ook in de gebouwde JavaScript.
# De beveiliging zit in row level security, niet in geheimhouding.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_KEY=<anon-key>
